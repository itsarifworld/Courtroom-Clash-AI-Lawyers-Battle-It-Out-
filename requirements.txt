fastapi                    # Web framework for backend API
uvicorn                    # ASGI server to run FastAPI
pydantic                   # Data validation (models/schemas)
numpy                      # For vector ops (planned in utils)
scikit-learn               # For TF-IDF and cosine sim (planned)
sentence-transformers      # For semantic embeddings (future)
python-dotenv              # Config via .env if needed later
